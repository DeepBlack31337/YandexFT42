        //После выполнения данных действий запускается второй механизм. Если это одно слово то нужно скачать аннотацию
        //поэтому думаю разбить приложение на несколько универсальных модулей
        //1. Модуль проверок системных событий
        //2. Модуль поиска слов и фраз
        //3. Модуль закачки из интернета
        //4. Модуль работы с моделью рилма
        //5. Модуль работы с файлами
        //6. Модуль работы с UI
        //7. Модуль работы с внешними компонентами, шара, озвучка и т.д.

        //Вешаю реакцию на изменение текста, ставлю задержку в 1 сек. для того что бы запросы не сыпались при каждом нажатии
        //модуль получится достаточно большой
        //Каждый пункт будет начинатся с проверок на доступность выполнения операций
        //1. Проверка есть ли данное слово, или слова в прошлых запросах базы кеша рилма, если есть предоставляем
        //2. Проверка есть ли подключенный словарь данного языка и если есть берем из него, если не находим 3.
        //3. Проверка на доступность инета, если прошла делаем запрос, пока запрос выполняется крутим прогресс на TextView
        //Вообще доступность инета можно проверять горячим обсерваблем, работающим пока выполняется запрос для своевременной отмены
        //например когда переключается моб.инет и вафля
        //4. Если запрос из интернета прошел нормально, нужно записать слово или фразу с переводом в таблицу кеша рилма

        //Состав таблицы кеша:
        //id - уникальный идентификатор
        //flow - направление перевода
        //first - начальная фраза, слово
        //second - переведенная фраза, слово
        //ann - аннотация если была загружена
        //izbr - избранное Да/Нет

        //Состав таблицы избранного: отдельная таблица вообще не нужна, оставлю пометку в кеше
        //id - уникальный идентификатор
        //flow - направление перевода
        //first - начальная фраза, слово
        //second - переведенная фраза, слово
        //ann - аннотация если была загружена

        //Состав таблицы настроек:
        //...будут добавлятся при добавлении настроек
